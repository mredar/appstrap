set daemon 30
#set logfile syslog facility log_daemon
set logfile {{ role_home_dir.stdout }}/servers/monit/log/monit.log

include {{ role_home_dir.stdout}}/.monit/*rc

set httpd port 28812
    allow localhost
    allow 127.0.0.1

