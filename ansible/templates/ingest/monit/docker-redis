check program docker_redis with path "/usr/bin/docker ps -q | grep 6fa8a2707981"
        every 1 cycles
	start program "/usr/bin/docker start redis"
        stop program "/usr/bin/docker stop redis"
	#if failed port 5984 then start
	if status != 0 then start
