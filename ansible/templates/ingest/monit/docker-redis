check program docker_redis with path "/usr/bin/docker ps -q | grep 0ae4447d81ae"
        every 1 cycles
	start program "/usr/bin/docker start 0ae4447d81ae"
        stop program "/usr/bin/docker stop 0ae4447d81ae"
	#if failed port 5984 then start
	if status != 0 then start
