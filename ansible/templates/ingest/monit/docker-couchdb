check program docker_couchdb with path "/usr/bin/docker ps -q | grep cbd4bb13b88f"
        every 1 cycles
	start program "/usr/bin/docker start couchdb"
        stop program "/usr/bin/docker stop couchdb"
	#if failed port 5984 then start
	if status != 0 then start
